SEL14 = SELECT * \
          FROM PRODUCT

SEL15 = SELECT * \
          FROM PRODUCT \
         WHERE NAME LIKE ?

INS14 = INSERT INTO PRODUCT \
          (NAME, PRICE) \
        VALUES \
          (?, ?)

SEL16 = SELECT COUNT(P.ID) COUNT \
          FROM PRODUCT P \
         WHERE P.NAME = ? 

SEL17 = SELECT P.ID, \
               P.NAME, \
               P.PRICE \
          FROM PRODUCT P \
         WHERE P.PRICE <= ? 
         
SEL18 = SELECT P.ID    ID,    \
               P.NAME  NAME,  \
               P.PRICE PRICE \
          FROM PRODUCT P
          
SEL19 = SELECT P.ID    ID, \
               P.NAME  NAME, \
               P.PRICE PRICE \
          FROM PRODUCT P \
         WHERE P.NAME LIKE ?
